import math

def generate_polygon_mesh(char, scale=50):
    # Placeholder: Retorna un cuadrado simple para cualquier letra
    return [
        (0, 0),
        (scale, 0),
        (scale, scale),
        (0, scale)
    ]
